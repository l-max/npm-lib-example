{
    "name": "npm-lib-example",
    "version": "1.0.0",
    "description": "Example library for use from github",
    "main": "src",
    "private": true,
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
